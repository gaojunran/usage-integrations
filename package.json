{
  "name": "usage-spec-monorepo",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "aube run build -r",
    "test": "aube run test -r",
    "test:watch": "aube run test:watch -r"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
